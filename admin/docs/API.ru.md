# Справочник по API
**语言 / Languages:** [中文](API.md) · [English](API.en.md) · [한국어](API.ko.md) · [Русский](API.ru.md) · [Deutsch](API.de.md) · [Français](API.fr.md) · [Español](API.es.md) · [Português](API.pt.md) · [हिन्दी](API.hi.md) · [العربية](API.ar.md) · [বাংলা](API.bn.md) · [Bahasa Indonesia](API.id.md) · [日本語](API.ja.md)

> Copyright (c) 2026 erik <erik@erik.xyz> — https://erik.xyz

## 1. Обзор

Административная панель open-admin построена на webman v2 и предоставляет RESTful JSON API. Все интерфейсы администрирования требуют JWT-аутентификации и проверки прав RBAC; публичные интерфейсы маршрутизируются в версионированные контроллеры через заголовок версии API.

- **Базовый URL**: `http://localhost:8787`
- **Версия API**: управляется заголовком `API-Version: v1` (по умолчанию v1, если заголовок отсутствует)
- **Язык**: переключается заголовком `Accept-Language` или параметром `?lang=zh_CN|en` (по умолчанию zh_CN), автоматически определяется middleware Locale

> **Обзор конечных точек**: аутентификация(5) | дашборд(1) | пользователи(7) | роли(4) | права(4) | настройки(4) | логи(1) | профиль(3) | импорт/экспорт(3) | загрузка(1) | эксплуатация(4: health/metrics/docs/security.txt) | всего 37 конечных точек
- **Аутентификация**: `Authorization: Bearer <token>` (JWT)
- **Формат ответа**: `{ "code": 0, "message": "success", "data": {...} }`
- **Документация**: `GET /api/docs` возвращает спецификацию OpenAPI 3.0 в формате JSON

### Требования к запросам

- Разрешены только методы `GET` / `POST` / `PUT` / `DELETE` / `OPTIONS` / `HEAD`; другие HTTP-методы (например, TRACE, CONNECT, PATCH) возвращают 405
- Все запросы `POST` / `PUT` должны содержать `Content-Type: application/json` (кроме загрузки файлов), иначе возвращается 415
- Размер тела запроса не должен превышать 10MB, иначе возвращается 413
- Фильтр безопасности сканирует все входные данные запроса на XSS, SQL-инъекции, обход пути и инъекции команд; при обнаружении возвращается 403
- 5 неудачных попыток входа подряд блокируют аккаунт (15 минут); во время блокировки запросы входа возвращают 429
- Один пользователь может одновременно владеть не более чем 3 действительными токенами; при превышении самый старый токен автоматически попадает в чёрный список

## 2. Коды ошибок

| code | Значение | Сценарий возникновения |
|------|------|---------|
| 0 | Успех | |
| 400 | Ошибка параметров запроса | Некорректный формат запроса |
| 401 | Не аутентифицирован | Токен отсутствует / истёк / в чёрном списке |
| 403 | Нет прав / блокировка безопасности | Недостаточно прав RBAC / сработал SecurityFilter |
| 404 | Ресурс не найден | Цель запроса/обновления/удаления не существует |
| 405 | Метод не разрешён | Разрешены только GET/POST/PUT/DELETE/OPTIONS/HEAD; нестандартные методы отклоняются |
| 413 | Тело запроса слишком большое | Content-Length превышает 10MB |
| 415 | Неподдерживаемый тип медиа | Content-Type запроса POST/PUT не является JSON и не является загрузкой файла |
| 422 | Ошибка валидации параметров | Отсутствуют обязательные поля, неверный формат, бизнес-проверка не пройдена |
| 429 | Слишком много запросов | Сработал RateLimit / блокировка аккаунта (5 неудачных входов подряд блокируют на 15 минут) |
| 500 | Внутренняя ошибка сервера | |

## 3. Публичные конечные точки

Все публичные конечные точки размещены в группе `/api`; middleware `ApiVersion` направляет их в версионированный контроллер в соответствии с заголовком `API-Version` (например, `app\api\v1\controller\AuthController`).

### 3.1 Проверка работоспособности

```
GET /health
```

- **Аутентификация**: не требуется
- **Ограничение частоты**: нет

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "app": "open-admin",
    "version": "1.0",
    "php": "8.3.0",
    "database": "ok",
    "redis": "ok",
    "elasticsearch": "ok",
    "timestamp": 1716249600
  }
}
```

Значения `database`, `redis`, `elasticsearch`: `"ok"` | `"unavailable"`. `elasticsearch` возвращает `"unavailable"`, если ES недоступен; если статус кластера не green/yellow, возвращается фактическое значение status (например, `"red"`).

### 3.2 Документация API

```
GET /api/docs
```

- **Аутентификация**: не требуется
- **Ограничение частоты**: глобальное по умолчанию (60/мин)
- **Ответ**: спецификация OpenAPI 3.0.3 в формате JSON, включая все определения конечных точек, параметры и схемы

### 3.3 Генерация капчи

```
POST /api/captcha/generate
```

- **Аутентификация**: не требуется
- **Заголовок запроса**: `API-Version: v1` (обязателен)
- **Ограничение частоты**: глобальное по умолчанию (60/мин)

**Тело запроса**:
```json
{
  "difficulty": "medium"
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| difficulty | string | Нет | `easy` / `medium` / `hard`, по умолчанию `medium` |

**Пример ответа** — клик-тип (`type: "click"`):
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "key": "abc123def456",
    "type": "click",
    "image": "data:image/png;base64,iVBORw0KGgo...",
    "extra": {
      "targets": [
        { "order": 1, "text": "A", "x": 120, "y": 85 },
        { "order": 2, "text": "B", "x": 310, "y": 42 }
      ]
    }
  }
}
```

**Пример ответа** — слайдер-тип (`type: "slider"`):
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "key": "def456abc789",
    "type": "slider",
    "image": "data:image/png;base64,iVBORw0KGgo...",
    "extra": {
      "x": 120,
      "y": 60,
      "puzzle_w": 50,
      "puzzle_h": 50,
      "puzzle": "data:image/png;base64,iVBORw0KGgo..."
    }
  }
}
```

**Пример ответа** — поворот-тип (`type: "rotate"`):
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "key": "ghi789abc012",
    "type": "rotate",
    "image": "data:image/png;base64,iVBORw0KGgo...",
    "extra": {
      "angle": 45
    }
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| key | string | Идентификатор капчи, передаётся обратно при проверке |
| type | string | Тип капчи: `click` / `slider` / `rotate` |
| image | string | Изображение в виде base64 data URI |
| extra | object | Дополнительные данные в зависимости от типа (см. ниже) |

**`extra` по типам**:

| type | поля extra | Тип | Описание |
|------|-----------|------|------|
| click | targets | array | Цели кликов: `order`(порядок) `text`(текст подсказки) `x` `y`(координаты) |
| slider | x, y | int | Координаты левого верхнего угла выреза (на холсте 300×200) |
| slider | puzzle_w, puzzle_h | int | Ширина и высота изображения-пазла |
| slider | puzzle | string | Изображение-пазл в виде base64 data URI |
| rotate | angle | int | Правильный угол поворота (0-359); нужно повернуть на `360-angle` для выравнивания изображения |

### 3.4 Проверка капчи

```
POST /api/captcha/verify
```

- **Аутентификация**: не требуется
- **Заголовок запроса**: `API-Version: v1` (обязателен)
- **Ограничение частоты**: глобальное по умолчанию (60/мин)

**Тело запроса** — клик-тип (`type: "click"`):
```json
{
  "key": "abc123def456",
  "type": "click",
  "clicks": [
    { "x": 120, "y": 85 },
    { "x": 310, "y": 42 }
  ]
}
```

**Тело запроса** — слайдер-тип (`type: "slider"`):
```json
{
  "key": "def456abc789",
  "type": "slider",
  "clicks": 120
}
```

**Тело запроса** — поворот-тип (`type: "rotate"`):
```json
{
  "key": "ghi789abc012",
  "type": "rotate",
  "clicks": 315
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| key | string | Да | Ключ капчи, возвращённый generate |
| type | string | Да | Тип капчи, должен совпадать с `type`, возвращённым generate |
| clicks | вариант | Да | Данные ответа, формат меняется в зависимости от type (см. ниже) |

**`clicks` по типам**:

| type | тип clicks | Описание | Допустимая погрешность |
|------|------------|------|---------|
| click | `[{x:int, y:int}]` | Массив координат кликов, в порядке order | радиус 18px |
| slider | `int` | Смещение слайдера по оси X | ±4px |
| rotate | `int` | Угол поворота (0-359) | ±5° |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "验证通过",
  "data": { "valid": true }
}
```

После успешной проверки сервер записывает `captcha_verified:{key}` в Redis (TTL 300с), и интерфейс входа пропускает запрос на основании этого.
При неудачной проверке `code` равен 422, `message` — `"验证失败，请重试"`, а `data.valid` — `false`.

### 3.5 Вход

```
POST /api/auth/login
```

- **Аутентификация**: не требуется
- **Заголовок запроса**: `API-Version: v1` (обязателен)
- **Ограничение частоты**: 10/мин (по IP + пути)

**Тело запроса**:
```json
{
  "username": "admin",
  "password": "djGYscnyS5V6mW6KyDFjB8vGwjBBnB3Odpyxu8LY...",
  "captcha_key": "abc123def456"
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| username | string | Да | min:3, max:50 | Имя пользователя |
| password | string | Да | min:6, max:32 (открытый текст) | Шифрование AES-256-CBC-HMAC, затем кодирование Base64 (совместимо с открытым текстом) |
| captcha_key | string | Да | | Ключ капчи (необходимо сначала пройти `/api/captcha/verify`) |

### Протокол шифрования пароля

Используется **асимметричное шифрование RSA-2048**; открытый ключ находится в коде фронтенда (его можно безопасно раскрывать), закрытый ключ хранится только на сервере.

```
Процесс шифрования (клиент):
  Открытый ключ RSA (PEM) → шифрование PKCS1v1.5 → кодирование Base64 → передача

Процесс расшифровки (сервер, поэтапный откат):
  1. Расшифровка закрытым ключом RSA → успех и корректный UTF-8 → использовать расшифрованный результат
  2. Расшифровка AES-256-CBC-HMAC → успех → использовать расшифрованный результат (совместимость со старыми клиентами)
  3. Откат к открытому тексту → использовать исходные данные напрямую
```

Открытый ключ встроен в приложение фронтенда и не требует передачи по сети. Закрытый ключ хранится только в `RSA_PRIVATE_KEY` в `.env` и не должен быть раскрыт.

> Симметричное шифрование AES — схема совместимости со старыми версиями; будет удалено после миграции всех клиентов на RSA.

**Пример ответа**:
```json
{
  "code": 0,
  "message": "登录成功",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 7200,
    "user": {
      "id": "a1b2c3d4",
      "username": "admin",
      "real_name": "管理员"
    }
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| access_token | string | JWT-токен доступа |
| refresh_token | string | JWT-токен обновления |
| expires_in | int | Срок действия токена доступа (секунды), по умолчанию 7200 |
| user.id | string | Идентификатор пользователя, зашифрованный hashid |
| user.username | string | Имя пользователя |
| user.real_name | string | Настоящее имя |

**Возможные ошибки**:
- 422: Ошибка валидации параметров (отсутствуют обязательные поля, неверный формат)
- 422: Сначала выполните проверку капчи (captcha_key не прошёл `/api/captcha/verify`)
- 401: Неверное имя пользователя или пароль
- 403: Аккаунт отключён
- 429: Аккаунт заблокирован, попробуйте через 15 минут (срабатывает после 5 неудачных входов подряд)

### 3.6 Регистрация

```
POST /api/auth/register
```

- **Аутентификация**: не требуется
- **Заголовок запроса**: `API-Version: v1` (обязателен)
- **Ограничение частоты**: 5/мин (по IP + пути)

**Тело запроса**:
```json
{
  "username": "newuser",
  "password": "djGYscnyS5V6mW6KyDFjB8vGwjBBnB3Odpyxu8LY...",
  "real_name": "新用户",
  "captcha_key": "abc123def456"
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| username | string | Да | min:3, max:50 | Имя пользователя (уникальное) |
| password | string | Да | min:6, max:32 (открытый текст) | Шифрование AES-256-CBC-HMAC, затем кодирование Base64 |
| real_name | string | Да | max:50 | Настоящее имя |
| captcha_key | string | Да | | Ключ капчи (необходимо сначала пройти `/api/captcha/verify`) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "注册成功",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 7200,
    "user": {
      "id": "e5f6g7h8",
      "username": "newuser",
      "real_name": "新用户"
    }
  }
}
```

После успешной регистрации сразу возвращаются JWT-токены; статус пользователя по умолчанию включён (status=1).

### 3.7 Обновление токена

```
POST /api/auth/refresh
```

- **Аутентификация**: не требуется
- **Заголовок запроса**: `API-Version: v1` (обязателен)
- **Ограничение частоты**: глобальное по умолчанию (60/мин)

**Тело запроса**:
```json
{
  "refresh_token": "eyJhbGciOiJIUzI1NiIs..."
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| refresh_token | string | Да | refresh_token, полученный при входе/регистрации |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "refresh_token": "eyJhbGciOiJIUzI1NiIs...",
    "expires_in": 7200
  }
}
```

При успешном обновлении возвращаются новые access_token и refresh_token; старые токены автоматически теряют силу. При обновлении обновляются время последнего входа и IP пользователя.

**Возможные ошибки**:
- 422: Отсутствует refresh_token
- 401: refresh_token недействителен или истёк

### 3.8 Метрики Prometheus

```
GET /metrics
```

- **Аутентификация**: не требуется
- **Ограничение частоты**: нет
- **Формат ответа**: Prometheus text format (`text/plain; version=0.0.4`)

Публичная конечная точка метрик Prometheus для сбора Grafana/Prometheus.

**Пример ответа**:
```
# HELP openadmin_http_requests_total Total number of HTTP requests.
# TYPE openadmin_http_requests_total gauge
openadmin_http_requests_total 15234

# HELP openadmin_active_users Number of active users.
# TYPE openadmin_active_users gauge
openadmin_active_users 156

# HELP openadmin_db_connection_status Database connection status (1=ok, 0=fail).
# TYPE openadmin_db_connection_status gauge
openadmin_db_connection_status 1

# HELP openadmin_redis_connection_status Redis connection status (1=ok, 0=fail).
# TYPE openadmin_redis_connection_status gauge
openadmin_redis_connection_status 1

# HELP openadmin_memory_usage_bytes Memory usage in bytes.
# TYPE openadmin_memory_usage_bytes gauge
openadmin_memory_usage_bytes 18874368
```

| Имя метрики | Тип | Описание |
|------|------|------|
| `openadmin_http_requests_total` | gauge | Суммарное количество HTTP-запросов |
| `openadmin_active_users` | gauge | Текущее количество активных пользователей (входивших за 24 часа) |
| `openadmin_db_connection_status` | gauge | Состояние подключения к БД, 1=норма, 0=ошибка |
| `openadmin_redis_connection_status` | gauge | Состояние подключения к Redis, 1=норма, 0=ошибка |
| `openadmin_memory_usage_bytes` | gauge | Текущее использование памяти процессом PHP (байты) |

## 4. Дашборд

Все интерфейсы администрирования размещены в группе `/admin` и проходят через три middleware: `AdminAuth` (JWT-аутентификация), `AdminPermission` (проверка прав RBAC) и `OperationLog` (запись операций).

### 4.1 Данные дашборда

```
GET /admin/dashboard
```

- **Аутентификация**: JWT + RBAC
- **Кэш**: Redis 5 минут

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "stats": [
      {
        "label": "用户总数",
        "value": "1280",
        "icon": "people",
        "color": "#1677FF",
        "trend": 12.5
      },
      {
        "label": "今日新增",
        "value": "23",
        "icon": "person_add",
        "color": "#52C41A"
      },
      {
        "label": "活跃用户",
        "value": "156",
        "icon": "bolt",
        "color": "#FA8C16"
      },
      {
        "label": "操作日志",
        "value": "432",
        "icon": "description",
        "color": "#722ED1"
      }
    ],
    "trends": {
      "dates": ["2026-04-22", "2026-04-23", "..."],
      "series": [
        { "name": "累计用户", "data": [1200, 1210, "..."], "color": "#1677FF" },
        { "name": "操作日志", "data": [45, 52, "..."], "color": "#52C41A" }
      ]
    },
    "distribution": {
      "user_status": [
        { "name": "启用", "value": 1250 },
        { "name": "禁用", "value": 30 }
      ]
    },
    "recent_logs": [
      {
        "id": "hashid...",
        "action": "用户登录",
        "method": "POST",
        "path": "/api/auth/login",
        "ip": "192.168.1.1",
        "user_name": "admin",
        "created_at": "2026-05-21 10:30:00"
      }
    ]
  }
}
```

| поля stats | Тип | Описание |
|------|------|------|
| label | string | Название метрики |
| value | string | Значение метрики (строковый тип) |
| icon | string | Имя иконки Material |
| color | string | Цвет карточки |
| trend | float? | Дневной темп роста (в процентах); есть только у "всего пользователей" |

| поля trends | Тип | Описание |
|------|------|------|
| dates | array{string} | Последовательность дат за последние 30 дней |
| series | array{object} | Данные линии тренда: name (название), data (массив значений), color (цвет) |

## 5. Управление пользователями

`id`, возвращаемый всеми интерфейсами управления пользователями, — строка, зашифрованная hashid. Поля пароля исключены из ответов. Телефоны и email маскируются в списках и возвращаются открытым текстом в деталях (зашифрованные поля БД автоматически расшифровываются trait Encryptable).

### 5.1 Список пользователей

```
GET /admin/user
```

- **Аутентификация**: JWT + RBAC

**Параметры запроса**:

| Параметр | Тип | Обязательно | По умолчанию | Описание |
|------|------|------|------|------|
| page | int | Нет | 1 | Номер страницы |
| limit | int | Нет | 15 | Количество записей на странице |
| keyword | string | Нет | | Ключевое слово поиска, совпадает с именем пользователя и настоящим именем |
| status | int | Нет | | Фильтр статуса, 0=отключён, 1=включён |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": "a1b2c3d4",
        "username": "admin",
        "real_name": "管理员",
        "phone": "138****5678",
        "email": "a***@example.com",
        "status": 1,
        "last_login_at": "2026-05-21 10:30:00",
        "created_at": "2026-01-01 00:00:00"
      }
    ],
    "total": 100,
    "page": 1,
    "limit": 15
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| id | string | Идентификатор пользователя, зашифрованный hashid |
| username | string | Имя пользователя |
| real_name | string | Настоящее имя |
| phone | string | Маскированный телефон (`138****5678` формат) |
| email | string | Маскированный email (`a***@example.com` формат) |
| status | int | 1=включён, 0=отключён |
| last_login_at | string | Время последнего входа (datetime) |
| created_at | string | Время создания (datetime) |

### 5.2 Создание пользователя

```
POST /admin/user
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "username": "newuser",
  "password": "123456",
  "real_name": "新用户",
  "phone": "13800138000",
  "email": "newuser@example.com",
  "status": 1
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| username | string | Да | min:3, max:50 | Имя пользователя (уникальное) |
| password | string | Да | min:6, max:32 | Пароль (хранится в bcrypt) |
| real_name | string | Да | max:50 | Настоящее имя |
| phone | string | Нет | | Телефон (шифруется через Encryptable) |
| email | string | Нет | | Email (шифруется через Encryptable) |
| status | int | Нет | in:0,1 | Статус, по умолчанию 1 (включён) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "创建成功",
  "data": {
    "id": "e5f6g7h8",
    "username": "newuser",
    "real_name": "新用户",
    "phone": "13800138000",
    "email": "newuser@example.com",
    "status": 1,
    "created_at": "2026-05-21 12:00:00"
  }
}
```

**Возможные ошибки**:
- 422: Имя пользователя уже существует
- 422: Ошибка валидации параметров (отсутствуют обязательные поля)

### 5.3 Детали пользователя

```
GET /admin/user/{id}
```

- **Аутентификация**: JWT + RBAC
- **Параметр пути**: `{id}` — идентификатор пользователя, зашифрованный hashid

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": "a1b2c3d4",
    "username": "admin",
    "real_name": "管理员",
    "phone": "13800138000",
    "email": "admin@example.com",
    "status": 1,
    "last_login_at": "2026-05-21 10:30:00",
    "last_login_ip": "192.168.1.1",
    "created_at": "2026-01-01 00:00:00",
    "updated_at": "2026-05-20 08:00:00"
  }
}
```

В детальном интерфейсе `phone` и `email` возвращаются открытым текстом (в БД хранятся зашифрованными, автоматически расшифровываются cast Encryptable) и не маскируются. `password` и `id_card` никогда не включаются в ответ.

**Возможные ошибки**:
- 404: Пользователь не найден

### 5.4 Обновление пользователя

```
PUT /admin/user/{id}
```

- **Аутентификация**: JWT + RBAC
- **Параметр пути**: `{id}` — идентификатор пользователя, зашифрованный hashid

**Тело запроса**:
```json
{
  "real_name": "新姓名",
  "password": "",
  "phone": "13900139000",
  "email": "new@example.com",
  "status": 1
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| real_name | string | Нет | Настоящее имя; если не передано, сохраняется прежнее значение |
| password | string | Нет | Новый пароль; не изменяется, если пустая строка или не передан |
| phone | string | Нет | Телефон |
| email | string | Нет | Email |
| status | int | Нет | 0=отключён, 1=включён |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "id": "a1b2c3d4",
    "username": "admin",
    "real_name": "新姓名",
    "phone": "13900139000",
    "email": "new@example.com",
    "status": 1,
    "updated_at": "2026-05-21 12:30:00"
  }
}
```

**Возможные ошибки**:
- 404: Пользователь не найден

### 5.5 Удаление пользователя

```
DELETE /admin/user/{id}
```

- **Аутентификация**: JWT + RBAC
- **Параметр пути**: `{id}` — идентификатор пользователя, зашифрованный hashid
- **Чувствительная операция**: требуется повторное подтверждение паролем

**Тело запроса**:
```json
{
  "password": "admin_password"
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| password | string | Да | Пароль текущего вошедшего пользователя (повторное подтверждение) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "删除成功",
  "data": []
}
```

Выполняется мягкое удаление (Eloquent SoftDeletes): данным присваивается deleted_at без физического удаления.

**Возможные ошибки**:
- 404: Пользователь не найден
- 422: Для чувствительной операции требуется подтверждение паролем (password пуст)
- 422: Ошибка проверки пароля (пароль не совпадает)

### 5.6 Массовое удаление пользователей

```
POST /admin/user/batch/destroy
```

- **Аутентификация**: JWT + RBAC
- **Чувствительная операция**: требуется повторное подтверждение паролем

**Тело запроса**:
```json
{
  "ids": ["a1b2c3d4", "e5f6g7h8"],
  "password": "admin_password"
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| ids | array{string} | Да | Массив идентификаторов пользователей, зашифрованных hashid |
| password | string | Да | Пароль текущего вошедшего пользователя (повторное подтверждение) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "删除成功",
  "data": {
    "count": 2
  }
}
```

Выполняется мягкое удаление; `data.count` — фактическое количество удалённых.

**Возможные ошибки**:
- 422: Выберите пользователей для удаления (ids пуст)
- 422: Недействительный ID (не удалось декодировать hashid)
- 422: Ошибка проверки пароля

### 5.7 Массовое включение/отключение пользователей

```
POST /admin/user/batch/status
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "ids": ["a1b2c3d4", "e5f6g7h8"],
  "status": 1
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| ids | array{string} | Да | Массив идентификаторов пользователей, зашифрованных hashid |
| status | int | Да | 0=отключён, 1=включён |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "批量启用成功",
  "data": {
    "count": 2
  }
}
```

message динамически меняется в зависимости от status: `"批量启用成功"` или `"批量禁用成功"`.

**Возможные ошибки**:
- 422: Выберите пользователей (ids пуст)
- 422: Недействительное значение статуса (status не 0 и не 1)

## 6. Управление ролями

### 6.1 Список ролей

```
GET /admin/role
```

- **Аутентификация**: JWT + RBAC

**Параметры запроса**:

| Параметр | Тип | Обязательно | По умолчанию | Описание |
|------|------|------|------|------|
| page | int | Нет | 1 | Номер страницы |
| limit | int | Нет | 15 | Количество записей на странице |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": "r1r2r3r4",
        "name": "超级管理员",
        "slug": "super_admin",
        "description": "拥有所有权限",
        "status": 1,
        "users_count": 3,
        "created_at": "2026-01-01 00:00:00"
      }
    ],
    "total": 5,
    "page": 1,
    "limit": 15
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| id | string | Идентификатор роли, зашифрованный hashid |
| name | string | Название роли |
| slug | string | Идентификатор роли (уникальный, используется для проверки прав) |
| description | string | Описание роли |
| status | int | 1=включён, 0=отключён |
| users_count | int | Количество пользователей с этой ролью |

### 6.2 Создание роли

```
POST /admin/role
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "name": "编辑员",
  "slug": "editor",
  "description": "内容编辑角色",
  "status": 1,
  "permission_ids": [1, 5, 12, 15]
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| name | string | Да | max:50 | Название роли |
| slug | string | Да | max:50 | Идентификатор роли |
| description | string | Нет | | Описание роли, по умолчанию пустая строка |
| status | int | Нет | | Статус, по умолчанию 1 |
| permission_ids | array{int} | Нет | | Массив идентификаторов прав (исходные INT ID, не hashid) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "创建成功",
  "data": {
    "id": "r5r6r7r8",
    "name": "编辑员",
    "slug": "editor",
    "description": "内容编辑角色",
    "status": 1
  }
}
```

### 6.3 Обновление роли

```
PUT /admin/role/{id}
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "name": "内容编辑",
  "description": "更新后的描述",
  "status": 1,
  "permission_ids": [1, 5, 12, 20]
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| name | string | Нет | Название роли |
| description | string | Нет | Описание |
| status | int | Нет | 0=отключён, 1=включён |
| permission_ids | array{int} | Нет | Массив идентификаторов прав; при передаче права роли синхронизируются (перезаписываются) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "id": "r5r6r7r8",
    "name": "内容编辑",
    "slug": "editor",
    "description": "更新后的描述",
    "status": 1
  }
}
```

### 6.4 Удаление роли

```
DELETE /admin/role/{id}
```

- **Аутентификация**: JWT + RBAC
- **Чувствительная операция**: требуется повторное подтверждение паролем

**Тело запроса**:
```json
{
  "password": "admin_password"
}
```

**Пример ответа**:
```json
{
  "code": 0,
  "message": "删除成功",
  "data": []
}
```

При удалении автоматически снимаются связи роли со всеми правами и пользователями, затем запись роли удаляется физически.

## 7. Управление правами

Права имеют древовидную структуру (самоссылка parent_id) и делятся на три типа. Интерфейс списка возвращает полное дерево прав.

### 7.1 Дерево прав

```
GET /admin/permission
```

- **Аутентификация**: JWT + RBAC

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": [
    {
      "id": "p1p2p3p4",
      "parent_id": "0",
      "name": "用户管理",
      "slug": "/admin/user",
      "type": 1,
      "icon": "people",
      "path": "/user",
      "sort": 1,
      "created_at": "2026-01-01 00:00:00",
      "children": [
        {
          "id": "p5p6p7p8",
          "parent_id": "p1p2p3p4",
          "name": "用户列表",
          "slug": "/admin/user/index",
          "type": 2,
          "icon": "",
          "path": "/user/index",
          "sort": 1
        }
      ]
    }
  ]
}
```

| Поле | Тип | Описание |
|------|------|------|
| id | string | Зашифровано hashid |
| parent_id | string | hashid родительского права; "0" обозначает корневой узел |
| name | string | Название права |
| slug | string | Идентификатор права (маршрута/кнопки) |
| type | int | 1=меню, 2=кнопка, 3=API |
| icon | string | Иконка меню (имя иконки Material) |
| path | string | Путь маршрута фронтенда |
| sort | int | Порядок сортировки (по возрастанию) |
| children | array? | Список дочерних прав (рекурсивно); отсутствует, если дочерних узлов нет |

### 7.2 Создание права

```
POST /admin/permission
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "parent_id": 0,
  "name": "系统设置",
  "slug": "/admin/config",
  "type": 1,
  "icon": "settings",
  "path": "/config",
  "sort": 99
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| parent_id | int | Нет | | ID родительского права (исходный тип INT), по умолчанию 0 |
| name | string | Да | max:50 | Название права |
| slug | string | Да | max:100 | Идентификатор права |
| type | int | Да | in:1,2,3 | 1=меню, 2=кнопка, 3=API |
| icon | string | Нет | | Иконка меню, по умолчанию пусто |
| path | string | Нет | | Путь маршрута фронтенда, по умолчанию пусто |
| sort | int | Нет | | Порядок сортировки, по умолчанию 0 |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "创建成功",
  "data": {
    "id": "p9p0a1b2",
    "parent_id": "0",
    "name": "系统设置",
    "slug": "/admin/config",
    "type": 1,
    "icon": "settings",
    "path": "/config",
    "sort": 99
  }
}
```

### 7.3 Обновление права

```
PUT /admin/permission/{id}
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "name": "系统配置",
  "icon": "tune",
  "path": "/system/config",
  "sort": 50
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| name | string | Нет | Название права |
| icon | string | Нет | Иконка |
| path | string | Нет | Путь маршрута |
| sort | int | Нет | Порядок сортировки |

### 7.4 Удаление права

```
DELETE /admin/permission/{id}
```

- **Аутентификация**: JWT + RBAC
- **Чувствительная операция**: требуется повторное подтверждение паролем

**Тело запроса**:
```json
{
  "password": "admin_password"
}
```

**Пример ответа**:
```json
{
  "code": 0,
  "message": "删除成功",
  "data": []
}
```

При удалении каскадно удаляются все дочерние права (записи, где `parent_id` равен ID текущего права), а также снимаются связи со всеми ролями.

## 8. Системные настройки

Системные настройки уникальны по комбинации `group` + `key`.

### 8.1 Список настроек

```
GET /admin/config
```

- **Аутентификация**: JWT + RBAC

**Параметры запроса**:

| Параметр | Тип | Обязательно | По умолчанию | Описание |
|------|------|------|------|------|
| page | int | Нет | 1 | Номер страницы |
| limit | int | Нет | 15 | Количество записей на странице |
| group | string | Нет | | Фильтр по группе настроек |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": "c1c2c3c4",
        "group": "system",
        "key": "site_name",
        "value": "开放管理后台",
        "type": "string",
        "description": "站点名称",
        "created_at": "2026-01-01 00:00:00"
      }
    ],
    "total": 20,
    "page": 1,
    "limit": 15
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| id | string | hashid |
| group | string | Группа настроек (например, `system`, `email`, `storage`) |
| key | string | Ключ настройки |
| value | string | Значение настройки |
| type | string | Подсказка типа значения (`string`, `integer`, `boolean`, `json` и т.д.) |
| description | string | Описание настройки |

### 8.2 Создание настройки

```
POST /admin/config
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "group": "email",
  "key": "smtp_host",
  "value": "smtp.example.com",
  "type": "string",
  "description": "SMTP 服务器地址"
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| group | string | Да | max:100 | Группа настроек |
| key | string | Да | max:100 | Ключ настройки (уникален в пределах группы) |
| value | string | Да | | Значение настройки |
| type | string | Нет | | Тип значения, по умолчанию `string` |
| description | string | Нет | | Описание настройки, по умолчанию пусто |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "创建成功",
  "data": {
    "id": "c5c6c7c8",
    "group": "email",
    "key": "smtp_host",
    "value": "smtp.example.com",
    "type": "string",
    "description": "SMTP 服务器地址"
  }
}
```

**Возможные ошибки**:
- 422: Настройка уже существует (та же group + key)

### 8.3 Обновление настройки

```
PUT /admin/config/{id}
```

- **Аутентификация**: JWT + RBAC

**Тело запроса**:
```json
{
  "value": "smtp.newhost.com",
  "type": "string",
  "description": "更新后的 SMTP 地址"
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| value | string | Нет | Обновить значение настройки |
| type | string | Нет | Обновить тип значения |
| description | string | Нет | Обновить текст описания |

### 8.4 Удаление настройки

```
DELETE /admin/config/{id}
```

- **Аутентификация**: JWT + RBAC
- **Чувствительная операция**: требуется повторное подтверждение паролем

**Тело запроса**:
```json
{
  "password": "admin_password"
}
```

Физическое удаление записи настройки.

## 9. Журнал операций

Журнал операций доступен только для чтения; middleware `OperationLog` автоматически пишет при каждом запросе POST/PUT/DELETE. Сохраняемые поля: `user_id`, `action`, `method`, `path`, `ip`, `source`, `input`.

### 9.1 Список журнала операций

```
GET /admin/log
```

- **Аутентификация**: JWT + RBAC

**Параметры запроса**:

| Параметр | Тип | Обязательно | По умолчанию | Описание |
|------|------|------|------|------|
| page | int | Нет | 1 | Номер страницы |
| limit | int | Нет | 15 | Количество записей на странице |
| user_id | int | Нет | | Точный фильтр по ID пользователя (исходный тип INT) |
| action | string | Нет | | Точный фильтр по действию |
| path | string | Нет | | Нечёткий фильтр по пути запроса |
| start_date | string | Нет | | Дата начала (формат Y-m-d) |
| end_date | string | Нет | | Дата окончания (формат Y-m-d) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "success",
  "data": {
    "list": [
      {
        "id": "l1l2l3l4",
        "user_name": "admin",
        "action": "用户登录",
        "method": "POST",
        "path": "/api/auth/login",
        "ip": "192.168.1.1",
        "source": "web",
        "input": "{\"username\":\"admin\"}",
        "created_at": "2026-05-21 10:30:00"
      }
    ],
    "total": 500,
    "page": 1,
    "limit": 15
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| id | string | hashid |
| user_name | string | Имя пользователя операции (через связь user; неаутентифицированные операции показывают "系统") |
| action | string | Описание действия |
| method | string | HTTP-метод (POST/PUT/DELETE) |
| path | string | Путь запроса |
| ip | string | IP клиента |
| source | string | Источник запроса |
| input | string | Параметры запроса в виде JSON-строки (без файлов) |
| created_at | string | Время операции (datetime) |

## 10. Личный кабинет

Интерфейсы личного кабинета требуют только JWT-аутентификации (без проверки прав RBAC — middleware `AdminPermission` должен добавить их в белый список).

### 10.1 Обновление личной информации

```
PUT /admin/profile
```

- **Аутентификация**: JWT

**Тело запроса**:
```json
{
  "real_name": "新姓名",
  "phone": "13800138000",
  "email": "me@example.com"
}
```

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| real_name | string | Нет | Настоящее имя |
| phone | string | Нет | Телефон (шифруется через Encryptable) |
| email | string | Нет | Email (шифруется через Encryptable) |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "id": "a1b2c3d4",
    "username": "admin",
    "real_name": "新姓名",
    "phone": "13800138000",
    "email": "me@example.com",
    "status": 1,
    "last_login_at": "2026-05-21 10:30:00"
  }
}
```

В ответе `phone` и `email` возвращаются открытым текстом; `password` и `id_card` исключены.

### 10.2 Смена пароля

```
PUT /admin/profile/password
```

- **Аутентификация**: JWT

**Тело запроса**:
```json
{
  "old_password": "current_pass",
  "new_password": "new_pass_123"
}
```

| Поле | Тип | Обязательно | Правило проверки | Описание |
|------|------|------|---------|------|
| old_password | string | Да | | Текущий пароль |
| new_password | string | Да | min:6, max:32 | Новый пароль |

**Пример ответа**:
```json
{
  "code": 0,
  "message": "密码修改成功",
  "data": []
}
```

**Возможные ошибки**:
- 422: Введите старый и новый пароли
- 422: Неверный старый пароль
- 422: Длина нового пароля 6-32 символа

### 10.3 Выход

```
POST /admin/profile/logout
```

- **Аутентификация**: JWT

**Тело запроса**: нет (без requestBody, токен читается из заголовка Authorization)

**Пример ответа**:
```json
{
  "code": 0,
  "message": "已登出",
  "data": []
}
```

Логика выхода: декодировать JWT и получить остаток срока действия (exp - now), записать md5-хэш токена в чёрный список Redis `jwt_blacklist:{md5}` с TTL = остаток срока действия. Токены из чёрного списка блокируются middleware `AdminAuth` с возвратом 401.

При отсутствии токена возвращается 401. Истёкшие/недействительные токены (декодирование вызывает исключение) всё равно считаются успешным выходом.

## 11. Импорт и экспорт

### 11.1 Экспорт Excel

```
POST /admin/export/excel
```

- **Аутентификация**: JWT + RBAC
- **Тип ответа**: загрузка файла (`application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`)

**Тело запроса**:
```json
{
  "table": "admin_user",
  "columns": ["username", "real_name", "phone", "status", "created_at"],
  "conditions": {
    "status": 1
  },
  "title": "用户列表导出"
}
```

| Поле | Тип | Обязательно | По умолчанию | Описание |
|------|------|------|------|------|
| table | string | Нет | `admin_user` | Имя таблицы для экспорта. Поддерживаются: `admin_user`, `operation_log`, `admin_role`, `system_config` |
| columns | array{string} | Нет | | Массив имён колонок для экспорта; пусто — все колонки таблицы |
| conditions | object | Нет | `{}` | Условия фильтрации, пары ключ-значение; непустые значения используются в WHERE |
| title | string | Нет | `数据导出` | Заголовок Excel (отображается как имя листа) |

**Поддерживаемые таблицы и колонки**:

| table | Доступные колонки |
|-------|-------|
| `admin_user` | id, username, real_name, phone, email, status, last_login_at, last_login_ip, created_at |
| `operation_log` | id, user_id, action, method, path, ip, created_at |
| `admin_role` | id, name, slug, description, status, created_at |
| `system_config` | id, group, key, value, type, description, created_at |

Чувствительные поля `phone`, `email`, `id_card` автоматически маскируются при экспорте. Лимит данных — 10000 строк. В Excel закрепляется первая строка и включается автофильтр.

### 11.2 Экспорт PDF

```
POST /admin/export/pdf
```

- **Аутентификация**: JWT + RBAC
- **Тип ответа**: загрузка файла (`application/pdf`, A4 альбомная)

**Тело запроса**:
```json
{
  "type": "dashboard",
  "title": "管理仪表盘报告",
  "data": {
    "stats": [
      { "label": "用户总数", "value": "1280" }
    ]
  }
}
```

Или табличный режим:
```json
{
  "type": "table",
  "title": "用户列表",
  "data": {
    "columns": ["用户名", "真实姓名", "状态"],
    "rows": [
      ["admin", "管理员", "启用"],
      ["editor", "编辑员", "启用"]
    ]
  }
}
```

| Поле | Тип | Обязательно | По умолчанию | Описание |
|------|------|------|------|------|
| type | string | Нет | `table` | Тип экспорта: `table` / `dashboard` |
| title | string | Нет | `数据导出` | Заголовок PDF |
| data | object | Нет | `{}` | Данные для экспорта |

При `type=dashboard` `data` должен содержать массив `stats` (рендерится карточками); при `type=table` `data` должен содержать массивы `columns` и `rows`.

Шаблон PDF содержит информацию об авторских правах и временную метку экспорта.

### 11.3 Импорт пользователей (Excel)

```
POST /admin/import/users
```

- **Аутентификация**: JWT + RBAC
- **Тип запроса**: `multipart/form-data` (загрузка файла)

**Поля формы**:

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| file | file | Да | Формат `.xlsx` или `.xls` |

**Требования к колонкам Excel**:

| Колонка | Обязательно | Описание |
|------|------|------|
| username | Да | Имя пользователя (уникальное) |
| password | Да | Пароль (хранится в виде bcrypt-хэша) |
| real_name | Да | Настоящее имя |
| phone | Нет | Телефон |
| email | Нет | Email |
| status | Нет | Статус, по умолчанию 1 |

Строка 1 — заголовки колонок (регистронезависимо), данные начинаются со строки 2.

**Пример ответа**:
```json
{
  "code": 0,
  "message": "导入完成",
  "data": {
    "total": 10,
    "success": 8,
    "failed": 2,
    "errors": [
      { "row": 3, "reason": "用户名为空" },
      { "row": 7, "reason": "用户名 zhangsan 已存在" }
    ]
  }
}
```

| Поле | Тип | Описание |
|------|------|------|
| total | int | Общее количество строк (без строки заголовка) |
| success | int | Количество успешно импортированных |
| failed | int | Количество неудачных |
| errors | array | Детали ошибок: row (номер строки Excel) и reason (причина) |

## 12. Загрузка файлов

```
POST /admin/upload
```

- **Аутентификация**: JWT + RBAC
- **Тип запроса**: `multipart/form-data`

**Поля формы**:

| Поле | Тип | Обязательно | Описание |
|------|------|------|------|
| file | file | Да | Загружаемый файл |

**Разрешённые типы файлов**: `jpg`, `jpeg`, `png`, `gif`, `pdf`, `xlsx`, `docx`
**Максимальный размер файла**: 10MB

**Пример ответа**:
```json
{
  "code": 0,
  "message": "上传成功",
  "data": {
    "url": "/upload/2026-05-21/abc123def456.png"
  }
}
```

Файлы хранятся в директориях по датам `public/upload/{Y-m-d}/`, имя файла — `md5(uniqid) + исходное расширение`. `url` — относительный путь от корня сайта.

**Возможные ошибки**:
- 422: Выберите файл (не загружен)
- 422: Неподдерживаемый тип файла
- 422: Размер файла не может превышать 10MB
- 500: Ошибка загрузки файла (недействительный файл)

## 13. Заголовки ответа

Все интерфейсы (инъекция на уровне глобального middleware) содержат следующие заголовки ответа:

| Заголовок | Описание |
|----|------|
| `X-RateLimit-Limit` | Верхний предел частоты (количество) |
| `X-RateLimit-Remaining` | Оставшееся количество запросов |
| `X-RateLimit-Reset` | Временная метка сброса окна ограничения |
| `Retry-After` | Возвращается только при срабатывании ограничения; рекомендуемые секунды ожидания |
| `X-Content-Type-Options` | `nosniff` (по умолчанию в webman, запрещает MIME-сниффинг) |
| `X-Frame-Options` | `DENY` (предоставляется CORS middleware/базовой конфигурацией webman) |

Детали ограничения частоты:
- Глобальный лимит по умолчанию: 60/мин / IP+путь
- Конечная точка входа `/api/auth/login`: 10/мин
- Конечная точка регистрации `/api/auth/register`: 5/мин
- Используется атомарный алгоритм скользящего окна Redis (Lua ZSET), исключающий гонки TOCTOU
- При недоступности Redis — fail open (пропуск), запросы не блокируются

## 14. Процесс аутентификации

Полная последовательность аутентификации:

```
1. Клиент отправляет POST /api/captcha/generate
   (Заголовок запроса: API-Version: v1)
    ↓
   Сервер возвращает: key + type(click|slider|rotate) + изображение base64 + extra(данные по типу)
   
2. Пользователь выполняет действие капчи (клик/перетаскивание/поворот), клиент собирает ответ
   
3. Клиент отправляет POST /api/captcha/verify
   (Заголовок запроса: API-Version: v1, Content-Type: application/json)
   Тело запроса: { key, type, clicks }
   - type=click:  clicks = [{x, y}, ...]        // массив координат
   - type=slider: clicks = 120                   // смещение по X
   - type=rotate: clicks = 315                   // угол поворота
    ↓
   Сервер:
   a. Читает данные captcha:key из хранилища (TTL 300с)
   b. Проверяет ответ по type (click: евклидово расстояние ≤18px / slider: ±4px / rotate: ±5°)
   c. Проверка пройдена → запись в Redis `captcha_verified:{key}` = 1 (TTL 300с)
   d. Проверка не пройдена → возврат 422, счётчик +1, после 3 попыток key аннулируется
    ↓
   Сервер возвращает: { valid: true/false }

4. Клиент отправляет POST /api/auth/login
   (Заголовок запроса: API-Version: v1, Content-Type: application/json)
   Тело запроса: { username, password(зашифрован), captcha_key }
    ↓
   Сервер:
   a. Валидация параметров → 422
   b. Проверка наличия captcha_verified:{key} → 422
   c. Удаление captcha_verified:{key} (одноразовое использование)
   d. Расшифровка пароля: EncryptionService::decrypt(password) → открытый текст
   e. Проверка учётных данных (password_verify) → 401
   f. Проверка статуса аккаунта → 403/429
   g. Выдача JWT (access + refresh) → 200
   h. Обновление last_login_at / last_login_ip
    ↓
   Клиент сохраняет: access_token, refresh_token, expires_in

5. Последующие запросы содержат JWT
   Заголовок запроса: Authorization: Bearer <access_token>
    ↓
   Middleware AdminAuth:
   a. Извлекает Bearer-токен
   b. Проверяет чёрный список (Redis jwt_blacklist:{md5}) → 401
   c. Декодирует JWT, проверяет срок действия → 401
   d. Устанавливает $request->adminId = поле sub
    ↓
   Middleware AdminPermission:
   a. Разбирает идентификатор права для маршрута ресурса
   b. Запрашивает роли пользователя → права ролей и сопоставляет
   c. Нет прав → 403
    ↓
   Controller обрабатывает запрос
    ↓
   Response + заголовки X-RateLimit-*

6. Обновление до истечения access token
   Клиент отправляет POST /api/auth/refresh
   Тело запроса: { refresh_token: "..." }
    ↓
   Сервер декодирует refresh_token → выдаёт новые access + refresh
    ↓
   Клиент обновляет локальные токены

7. Выход
   Клиент отправляет POST /admin/profile/logout
   Заголовок запроса: Authorization: Bearer <access_token>
    ↓
   Сервер:
   a. Декодирует JWT и получает остаток TTL
   b. Запись в чёрный список Redis: jwt_blacklist:{md5(token)} = 1, TTL = остаток срока действия
   c. Возврат успеха
```

### Структура JWT

- **access_token**: `{ sub: <user_id>, username: "<name>" }`, TTL по умолчанию 7200 секунд (управляется конфигурацией JWT `default_expire`)
- **refresh_token**: `{ sub: <user_id>, token_type: "refresh" }`, TTL по умолчанию 1209600 секунд (управляется конфигурацией JWT `refresh_expire`, т.е. 14 дней)

### Управление безопасностью

- Пароли хранятся в виде хэшей `PASSWORD_BCRYPT`
- Пароли при передаче шифруются AES-256-CBC-HMAC (клиент шифрует → сервер расшифровывает), с откатом к открытому тексту
- Чувствительные поля (phone, email, id_card) прозрачно шифруются/расшифровываются на уровне БД через `erikwang2013/encryptable`
- ID на уровне API передаются зашифрованными через `erikwang2013/hashids`, что исключает раскрытие исходной последовательности snowflake ID
- SecurityFilter глобально сканирует XSS, SQL-инъекции, обход пути и инъекции команд; один IP 5 раз/60с → временный чёрный список на 15 минут
- Чувствительные операции (удаление пользователей, ролей, прав, настроек) требуют повторного подтверждения паролем текущего пользователя
- Ограничение одновременных сессий: не более 3 действительных токенов на пользователя; при входе с 4-го устройства самый старый токен принудительно попадает в чёрный список
- Блокировка аккаунта: 5 неудачных входов подряд вызывают блокировку на 15 минут; в течение блокировки возвращается 429

## 15. Развёртывание и эксплуатация

### Docker Compose

В корне проекта находится `docker-compose.yml`, оркестрирующий 5 сервисов (Nginx, webman app, MySQL, Redis, Elasticsearch). PHP собирается через `Dockerfile` (на базе `php:8.3-cli`, с включённым OPcache).

```bash
cp .env.docker .env
docker-compose up -d
```

### CI/CD

`.github/workflows/ci.yml` определяет конвейер непрерывной интеграции GitHub Actions:
- Проверка синтаксиса `php -l`
- Модульные тесты PHPUnit
- Статический анализ `flutter analyze`

### Резервное копирование базы данных

Директория `database/backup/` содержит скрипты резервного копирования и восстановления:
- `backup.sh` — резервное копирование mysqldump + gzip, автоматическая очистка резервных копий старше 30 дней
- `restore.sh` — интерактивное восстановление со списком доступных резервных копий

### Конфигурация безопасности Nginx

Для производственного развёртывания настройте усиление безопасности обратного прокси согласно `docs/nginx-security.conf`.
